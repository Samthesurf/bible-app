{
  "name": "bible-app",
  "version": "0.1.0",
  "description": "Offline desktop Bible reader - Bible.com style UI with 56 translations",
  "main": "dist-electron/main.js",
  "author": "Samuel Ukpai",
  "license": "MIT",
  "scripts": {
    "dev": "vite",
    "build:renderer": "vite build",
    "build:electron": "tsc -p tsconfig.node.json",
    "start": "npm run build:electron && electron .",
    "dev:electron": "npm run build:electron && (npm run dev &) && sleep 3 && electron .",
    "build": "npm run build:electron && npm run build:renderer && electron-builder",
    "build:dir": "npm run build:electron && npm run build:renderer && electron-builder --dir"
  },
  "dependencies": {
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^4.5.0",
    "electron": "^36.0.0",
    "electron-builder": "^26.0.0",
    "typescript": "^5.8.0",
    "vite": "^7.0.0"
  },
  "allowScripts": {
    "electron@36.9.5": true,
    "esbuild@0.28.1": true,
    "electron-winstaller@5.4.0": true
  }
}
